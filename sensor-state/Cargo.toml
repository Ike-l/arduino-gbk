[package]
name = "sensor-state"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]
test = false
doctest = false

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
heapless = "0.9.3"
